version: '3.8'

services:
  fauxx:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: fauxx
    ports:
      - "8000:8000"
    volumes:
      - fauxx_data:/data
    environment:
      DATA_DIR: /data
    restart: unless-stopped

volumes:
  fauxx_data:
